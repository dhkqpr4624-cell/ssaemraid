# 보스전 정답 판정 UI 동기화 수정

수정 파일:

- `app/student/boss-battle/page.tsx`

## 원인

학생 화면의 안전 폴링이 해당 라운드에서 답안이 아직 없을 때도 `answerHydratedRound`를 현재 라운드로 고정했습니다.
그 뒤 학생이 답안을 제출해도 같은 라운드의 답안을 다시 조회하지 않아, 서버에는 `is_correct = true`로 저장되었지만 학생 UI의 `currentAnswer`는 비어 있었습니다.

그 결과 서버 전투 계산은 정답으로 처리하면서도 학생 화면만 오답, 가위바위보 미참여, 방어 실패로 표시될 수 있었습니다.

## 수정

1. 답안이 실제로 확인된 경우에만 해당 라운드를 불러오기 완료로 표시합니다.
2. 제출 성공 직후 답안과 `isCorrect`를 학생 로컬 상태에 즉시 반영합니다.
3. 서버 재조회 전에도 정답 표시, 가위바위보 참여, 방어 성공 및 방어막 연출이 서버 판정과 일치합니다.

Supabase SQL 변경은 필요하지 않습니다.
