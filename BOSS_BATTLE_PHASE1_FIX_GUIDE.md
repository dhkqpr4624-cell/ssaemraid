# 보스전 1차 대기실 보완 수정

## 주요 변경
- 보스 선택 팝업을 최대 1500px / 화면 97vw까지 확장하고, 좁은 화면에서는 세로 배치
- 교사/학생이 대시보드로 나가도 진행 중 세션이 있으면 `보스전 이어서 참여하기` 표시
- 교사 대기실에 `보스전 끝내기` 추가 및 종료 확인창 적용
- 종료 시 세션을 `ended` 처리하고 참가자 데이터를 제거하여 학생 대시보드 입장 버튼 즉시 제거
- 참가자 PK를 출석번호가 아닌 학생 ID 기준으로 변경하여 여러 기기/학생 동시 접속 안정화
- 참가자 heartbeat 12초, 조회 2.5초, 비활성 판정 60초
- 실제 학생의 avatarState + inventory를 Sprite 모드로 렌더링
- 인원수에 따라 겹침을 줄이는 격자형 원근 배치 적용
- 교사/학생의 대시보드·나가기 버튼을 흰 배경/검은 글자로 수정
- 교사 대시보드 보스전 패널을 오른쪽 `최근 활동` 아래로 이동
- 설명에 `단원 마무리·학기 마무리` 문구 추가

## Supabase
기존 SQL을 실행한 경우에도 `supabase_boss_battle_phase1_migration.sql`을 다시 실행해야 합니다.
`student_id` 열과 새 복합 PK를 추가하는 갱신 SQL이 포함되어 있습니다.

## 결과 화면 관련
현재 1차는 대기실 단계이므로 실제 전투 결과 데이터는 아직 생성되지 않습니다.
세션 상태에는 향후 `result_ready`를 사용할 수 있도록 구조를 열어 두었으며,
2차 전투 구현 시 교사의 `결과 화면으로` 동작에서 세션을 닫고 결과/보상 화면으로 이동하도록 연결합니다.
