# 스쿨 레이드 15차 전투 밸런스 패치

## 목표

문항별 평균 정답률이 50~70%인 학급을 기준으로, 가위바위보 결과까지 포함했을 때 장기적으로 다음 체감을 목표로 합니다.

- 보스 처치: 약 25%
- 보스 도망: 약 50%
- 학생 전멸: 약 25%

실제 결과는 문제 수, 공격/방어 문제 배치, 학생별 정답 편차와 가위바위보 운에 따라 달라질 수 있습니다.

## 적용 내용

### 1. 보스 최대 HP 계산식 수정

`lib/boss-battle.ts`

- 평균 정답률 기준: 60%
- 가위바위보 기대 배율: `(2 + 1 + 0.5) / 3 = 7/6`
- 예상 총 피해량 대비 HP 계수: `1.08`
- 참가 인원 보정:
  - 1~10명: 보정 없음
  - 11~20명: HP +3%
  - 21~30명: HP +5%
  - 31~40명: HP +8%

### 2. 보스 공격력 약 8% 감소

`app/teacher/boss-battle/page.tsx`

방어 문제 전체에서 받는 기준 피해량을 `130`에서 `120`으로 낮췄고, 최소·최대 피해 범위도 함께 완화했습니다.

### 3. 보스 회복량 15% 감소

- 학생 1명당 회복 비율: 최대 HP의 `0.20%` → `0.17%`
- 한 번의 회복 상한: 최대 HP의 `2.0%` → `1.7%`

### 4. 진행도 체력 하한 완화

마지막 문제 전 보스가 조기 사망하는 것은 계속 막지만, 남은 공격 문항 비율에 따른 체력 하한 계수를 `0.72`에서 `0.50`으로 낮췄습니다. 따라서 마지막 공격 문제에서 처치와 도망 결과가 더 자연스럽게 갈립니다.

## 배포 방법

### GitHub

아래 두 파일만 기존 파일에 덮어써도 됩니다.

- `lib/boss-battle.ts`
- `app/teacher/boss-battle/page.tsx`

패치 설명도 저장하려면 다음 파일을 함께 올립니다.

- `BALANCE_PATCH_15_GUIDE.md`

전체 수정본 ZIP을 통째로 GitHub에 덮어써도 되지만, `node_modules`, `.next`, `.env.local`은 업로드하지 마세요.

### Supabase

이번 패치는 계산 로직만 변경하므로 **SQL Editor에서 실행할 내용이 없습니다.** 테이블 구조, RPC, RLS 정책 변경도 없습니다.

### 적용 시점

이미 생성되어 대기 중인 방은 기존 `bossMaxHp`가 저장되어 있을 수 있습니다. 배포 후 교사 화면에서 새 방을 만들거나 전투 시작 버튼을 누르면 참가 인원과 공격 문제 수를 기준으로 새 HP가 다시 계산됩니다.
