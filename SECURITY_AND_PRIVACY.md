# 쌤퀘스트(SAMQUEST) - 보안 및 개인정보 보호 정책

이 문서는 쌤퀘스트의 보안 아키텍처, 개인정보 보호 정책, 그리고 데이터 관리 방식을 설명합니다.

---

## 📋 목차

1. [개인정보 보호 원칙](#1-개인정보-보호-원칙)
2. [데이터 수집 정책](#2-데이터-수집-정책)
3. [인증 및 접근 제어](#3-인증-및-접근-제어)
4. [데이터 암호화](#4-데이터-암호화)
5. [Row Level Security (RLS)](#5-row-level-security-rls)
6. [배포 보안](#6-배포-보안)
7. [감사 및 모니터링](#7-감사-및-모니터링)

---

## 1. 개인정보 보호 원칙

쌤퀘스트는 **개인정보 보호를 최우선**으로 설계되었습니다.

### 1.1 수집하지 않는 정보

다음 정보는 **절대 수집하지 않습니다**:

| 정보 | 이유 |
|------|------|
| 학생 이름 | 개인 식별 정보 |
| 이메일 주소 | 개인 연락처 정보 |
| 전화번호 | 개인 연락처 정보 |
| 주소 | 개인 위치 정보 |
| 학년/반 | 학생 개인 정보 |
| 부모 정보 | 가족 정보 |
| 의료 정보 | 민감한 개인 정보 |

### 1.2 수집하는 정보

다음 정보만 수집합니다:

| 정보 | 용도 | 보관 기간 |
|------|------|---------|
| 클래스 코드 | 클래스 식별 | 클래스 삭제 시까지 |
| 출석번호 | 학생 식별 (클래스 내에서만) | 클래스 삭제 시까지 |
| 퀴즈 점수 | 학습 진행 추적 | 클래스 삭제 시까지 |
| 퀴즈 답변 | 채점 및 피드백 | 클래스 삭제 시까지 |
| 아바타 커스터마이징 | 게임화 기능 | 클래스 삭제 시까지 |
| 방 꾸미기 데이터 | 게임화 기능 | 클래스 삭제 시까지 |

---

## 2. 데이터 수집 정책

### 2.1 학생 데이터 수집

학생이 쌤퀘스트에 입장할 때:

1. **클래스 코드** 입력 (선생님이 제공)
2. **출석번호** 입력 (선생님이 할당)
3. 이 두 정보만으로 입장 가능

**이름, 이메일, 전화번호 등 추가 정보 입력 없음**

### 2.2 교사 데이터 수집

교사가 쌤퀘스트에 입장할 때:

1. **클래스 코드** 입력
2. **교사용 비밀번호** 입력
3. 이 두 정보만으로 입장 가능

**개인 정보 입력 없음**

### 2.3 데이터 저장소

모든 데이터는 다음 위치에 저장됩니다:

- **프로덕션**: Supabase PostgreSQL 데이터베이스 (암호화됨)
- **로컬 개발**: 브라우저 localStorage (개발 목적)

---

## 3. 인증 및 접근 제어

### 3.1 학생 인증

```
학생 입장 = 클래스 코드 + 출석번호
```

- 클래스 코드: 6자리 영문/숫자 (선생님이 공유)
- 출석번호: 1~50 (선생님이 할당)
- 이 조합은 해당 클래스 내에서만 유효

### 3.2 교사 인증

```
교사 입장 = 클래스 코드 + 교사용 비밀번호
```

- 클래스 코드: 6자리 영문/숫자 (고유)
- 교사용 비밀번호: 선생님이 설정 (해시 저장)

### 3.3 세션 관리

- **로컬 저장소**: 브라우저 세션 유지 (localStorage)
- **서버 검증**: Supabase RLS 정책으로 데이터 접근 제어

---

## 4. 데이터 암호화

### 4.1 전송 중 암호화 (In Transit)

- **HTTPS**: 모든 통신은 TLS 1.3 이상으로 암호화
- **Supabase**: 자동으로 HTTPS 적용
- **Vercel**: 자동으로 HTTPS 적용

### 4.2 저장 중 암호화 (At Rest)

#### 교사 비밀번호
```javascript
// 저장 시
const hashedPassword = await bcrypt.hash(password, 10);

// 검증 시
const isValid = await bcrypt.compare(inputPassword, hashedPassword);
```

- **알고리즘**: bcryptjs (NIST 승인)
- **Salt Rounds**: 10
- **평문 저장 금지**: 절대 평문으로 저장하지 않음

#### 학생 데이터
- Supabase 자동 암호화
- PostgreSQL 데이터베이스 레벨 암호화

---

## 5. Row Level Security (RLS)

### 5.1 RLS 정책 개요

Supabase RLS는 데이터베이스 레벨에서 접근 제어를 수행합니다.

### 5.2 클래스별 데이터 격리

각 테이블에 RLS 정책 적용:

| 테이블 | 정책 | 설명 |
|--------|------|------|
| `classes` | 공개 읽기 | 클래스 존재 확인용 |
| `students` | class_code 기반 | 같은 클래스 학생만 접근 |
| `quizzes` | class_code 기반 | 같은 클래스 퀴즈만 접근 |
| `quiz_results` | class_code 기반 | 같은 클래스 결과만 접근 |
| `live_sessions` | class_code 기반 | 같은 클래스 세션만 접근 |

### 5.3 RLS 정책 예시

```sql
-- students 테이블 조회 정책
CREATE POLICY "Allow read access to students by class_code" 
ON public.students 
FOR SELECT 
USING (class_code = request.jwt() ->> 'class_code');

-- 효과: 다른 클래스의 학생 데이터에 접근 불가
```

### 5.4 RLS 활성화 확인

Supabase 대시보드:
1. **Database** → **Tables**
2. 각 테이블 선택 → **RLS** 섹션
3. **Enable RLS** 토글 확인 (활성화 상태)

---

## 6. 배포 보안

### 6.1 GitHub 저장소 보안

#### Private 저장소
- 저장소는 **Private**으로 설정
- 승인된 사용자만 접근 가능

#### .gitignore 설정
```
# 절대 커밋하지 마세요
.env
.env.local
.env.*.local
*.key
*.pem
secrets.json
credentials.json
```

#### 커밋 히스토리 검토
- 민감한 정보가 커밋되지 않았는지 확인
- 실수로 커밋된 경우 즉시 키 재생성

### 6.2 Vercel 배포 보안

#### 환경 변수 관리
- **공개 변수**: `NEXT_PUBLIC_*` 접두사 (클라이언트에서 접근 가능)
- **비공개 변수**: 접두사 없음 (서버에서만 접근)

#### 필수 환경 변수
```
NEXT_PUBLIC_SUPABASE_URL=https://...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
```

#### 금지된 환경 변수
```
# 절대 설정하지 마세요
SUPABASE_SERVICE_ROLE_KEY=...
DATABASE_PASSWORD=...
```

### 6.3 Supabase 보안

#### API 키 관리
- **anon key**: 클라이언트에서 사용 (제한된 권한)
- **service role key**: 서버에서만 사용 (전체 권한)

#### RLS 정책
- 모든 테이블에 RLS 활성화
- 클래스 코드 기반 접근 제어

---

## 7. 감사 및 모니터링

### 7.1 로그 확인

#### Vercel 로그
- Vercel 대시보드 → **Deployments** → **Logs**
- 배포 오류 및 런타임 오류 확인

#### Supabase 로그
- Supabase 대시보드 → **Logs**
- 데이터베이스 쿼리 및 오류 확인

### 7.2 보안 감시

#### 정기 점검 항목
- [ ] 환경 변수가 올바르게 설정되었는가?
- [ ] RLS 정책이 활성화되었는가?
- [ ] 교사 비밀번호가 해시되어 저장되는가?
- [ ] 개인정보가 수집되지 않는가?
- [ ] HTTPS가 활성화되었는가?

### 7.3 인시던트 대응

개인정보 유출 의심 시:

1. **즉시 배포 중단**: Vercel에서 배포 중단
2. **Supabase 확인**: 비정상 접근 여부 확인
3. **로그 분석**: 접근 로그 검토
4. **키 재생성**: 필요시 API 키 재생성
5. **사용자 알림**: 영향받은 사용자에게 알림

---

## 📋 보안 체크리스트

배포 전 다음을 확인하세요:

### 코드 레벨
- [ ] `.env` 파일이 `.gitignore`에 포함되었는가?
- [ ] 비밀키가 코드에 하드코딩되지 않았는가?
- [ ] 교사 비밀번호가 bcryptjs로 해시되는가?

### Supabase 레벨
- [ ] 모든 테이블에 RLS가 활성화되었는가?
- [ ] RLS 정책이 클래스 코드 기반인가?
- [ ] 서비스 역할 키가 공개되지 않았는가?

### Vercel 레벨
- [ ] 환경 변수가 모두 설정되었는가?
- [ ] 민감한 정보가 공개 변수로 설정되지 않았는가?
- [ ] 배포 로그에 민감한 정보가 노출되지 않았는가?

### 운영 레벨
- [ ] 정기적으로 로그를 확인하는가?
- [ ] 비정상 접근을 모니터링하는가?
- [ ] 인시던트 대응 계획이 있는가?

---

## 📚 참고 자료

- [Supabase 보안 가이드](https://supabase.com/docs/guides/security)
- [Vercel 환경 변수](https://vercel.com/docs/concepts/projects/environment-variables)
- [OWASP 보안 가이드](https://owasp.org/)
- [bcryptjs 문서](https://github.com/dcodeIO/bcrypt.js)

---

**쌤퀘스트는 학생의 개인정보를 보호하기 위해 최선을 다합니다. 🔐**
