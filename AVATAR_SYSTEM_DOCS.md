# 아바타 및 아이템 시스템 업데이트 내역

## 1. 아이템 구조 확장 (Image-Ready)
- **이미지 지원**: 이모지뿐만 아니라 Base64 이미지(아이콘, 레이어용)를 지원합니다.
- **슬롯 시스템**: 아바타 장착 부위(머리, 눈, 얼굴, 상의, 하의, 신발, 모자, 액세서리, 배경)를 구분합니다.
- **희귀도**: Common, Rare, Epic, Legendary 등급을 시각적으로 구분합니다.

## 2. 아바타 꾸미기 (Avatar Customization)
- **레이어 렌더링**: 10단계의 레이어 구조로 아이템이 겹쳐서 표시됩니다.
  - `Background` -> `Body` -> `Bottom` -> `Shoes` -> `Top` -> `Face` -> `Eyes` -> `Hair` -> `Hat` -> `Accessory`
- **피부색 설정**: 5가지 프리셋 피부색을 실시간으로 변경하고 저장할 수 있습니다.
- **장착/해제 로직**: 슬롯당 하나의 아이템만 장착 가능하며, 장착 상태는 `localStorage`에 즉시 반영됩니다.

## 3. 교사용 보상 설정 개선
- **이미지 업로드**: 퀴즈 생성 시 보상 아이템 이미지를 직접 업로드할 수 있습니다.
- **미리보기**: 업로드한 이미지나 선택한 이모지를 즉시 확인할 수 있습니다.
- **슬롯 지정**: 아바타 파츠의 경우 어떤 부위에 장착할지 지정할 수 있습니다.

## 4. 학생 대시보드 통합
- **아바타 표시**: 대시보드 상단에 학생의 현재 아바타가 실시간으로 렌더링됩니다.
- **꾸미기 바로가기**: 프로필 옆의 '옷' 아이콘이나 하단 버튼을 통해 꾸미기 페이지로 이동합니다.

## 개발자 참고 사항
- **이미지 경로**: 현재는 Base64로 저장되지만, 나중에 `imageUrl` 필드를 Supabase Storage URL로 교체하기 쉽도록 설계되었습니다.
- **PNG 파츠**: `AvatarRenderer.tsx` 내의 TODO 주석 위치에 실제 PNG 파츠 렌더링 로직을 추가하면 됩니다.
