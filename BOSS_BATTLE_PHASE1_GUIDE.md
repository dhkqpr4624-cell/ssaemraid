# 보스전 1차: 대기실

## 적용 기능
- 교사 대시보드 `보스전 시작하기`
- 학생 대시보드 `보스전 참여하기`
- 타락한 해태 전용 대기실
- 문제 수/문제당 제한시간 설정
- 여러 퀴즈의 문항을 섞어 직접 선택
- 미선택 시 5학년 1학기 사회 3단원 문항 우선 무작위 선택
- 최대 40명 참가
- 현재 장착 상태 그대로 학생 아바타 표시
- 참가자 heartbeat를 별도 행으로 저장하여 동시 접속 덮어쓰기 방지
- 60초 이상 heartbeat가 없는 접속자는 자동 제외
- 참여 인원·문제 수 기반 보스 최대 HP 계산

## 밸런스
- 학생 기본 공격력 10
- 학생 기본 HP 100
- 예상 정답 집단: 참가자의 90%, 문제의 평균 80% 정답
- 보스 HP는 예상 총 피해량의 약 92%로 산정
- 다음 전투 단계용 공격 수치: 일반 공격 10%, 일반 공격 10%, 큰 공격 35%

## Supabase
`supabase_boss_battle_phase1_migration.sql`을 SQL Editor에서 1회 실행해야 합니다.

## 주의
이번 1차에서는 교사가 게임 시작을 누르면 세션 상태를 `starting`으로 전환하는 데까지 구현했습니다. 보스 등장/전투 화면은 2차에서 연결합니다.

## 보스 선택 팝업 추가

교사 대시보드의 `보스전 시작하기` 버튼은 곧바로 대기실로 이동하지 않습니다.

1. 3~6학년, 1~2학기, 1~3단원을 검색·필터링합니다.
2. 이용 가능한 보스를 선택합니다.
3. `선택하고 입장`을 누르면 해당 보스의 대기실로 이동합니다.

현재 이용 가능한 보스는 `5학년 1학기 사회 3단원 - 타락한 해태`입니다. 나머지 23개 단원은 `준비 중`으로 표시되며 클릭할 수 없습니다.

보스 목록은 `lib/boss-catalog.ts`에서 관리합니다. 이후 보스를 추가할 때 해당 항목의 `enabled`를 `true`로 바꾸고 이름·설명·이미지 정보를 확장하면 됩니다.
