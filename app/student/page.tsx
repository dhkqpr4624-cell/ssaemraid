'use client';
import {Suspense, useState} from 'react';import {useRouter, useSearchParams} from 'next/navigation';import {getBossBattleSession} from '@/lib/boss-battle';import {createRaidGuest,findRaidGuestByNickname,saveRaidGuestSession} from '@/lib/raid-guests';import {randomRaidAvatar} from '@/lib/raid-avatar';import {Button} from '@/components/ui/button';import {Input} from '@/components/ui/input';
function StudentJoinContent(){const r=useRouter(),sp=useSearchParams();const [step,setStep]=useState(1),[code,setCode]=useState((sp.get('code')||'').toUpperCase()),[nickname,setNickname]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function check(){setBusy(true);setError('');const s=await getBossBattleSession(code);setBusy(false);if(!s||s.status==='ended'){setError('방 코드를 확인해주세요. 아직 방이 만들어지지 않았거나 종료된 방입니다.');return;}setCode(code.toUpperCase());setStep(2)}async function join(){if(!nickname.trim()){setError('닉네임을 입력해주세요.');return}setBusy(true);try{const existing=await findRaidGuestByNickname(code,nickname);let g=existing;if(!g){const a=randomRaidAvatar();g=await createRaidGuest(code,nickname,a.state,a.items);}saveRaidGuestSession(g);r.push(`/student/boss-battle?code=${code}`)}catch(e:any){setError(e.message||'입장하지 못했습니다.')}finally{setBusy(false)}}return <main className="min-h-screen bg-slate-950 p-6 text-white flex items-center justify-center"><div className="w-full max-w-md rounded-2xl border border-slate-700 bg-slate-900 p-7 shadow-2xl"><img src="/school-raid-logo.png" className="mx-auto mb-4 h-32 object-contain"/><h1 className="mb-5 text-center text-2xl font-black">{step===1?'방 코드 입력':'닉네임 설정'}</h1>{step===1?<><Input value={code} onChange={e=>setCode(e.target.value.toUpperCase().replace(/[^A-Z0-9]/g,'').slice(0,6))} placeholder="6자리 방 코드" className="h-14 text-center text-2xl font-black tracking-[.25em] text-black"/><Button disabled={busy||code.length!==6} onClick={check} className="mt-4 w-full py-6 text-lg">입장</Button></>:<><Input value={nickname} onChange={e=>setNickname(e.target.value.slice(0,12))} placeholder="닉네임" className="h-14 text-center text-xl text-black"/><Button disabled={busy} onClick={join} className="mt-4 w-full py-6 text-lg">입장</Button></>}{error&&<p className="mt-4 text-center text-sm text-red-400">{error}</p>}</div></main>}

export default function StudentJoin(){return <Suspense fallback={<main className="min-h-screen bg-slate-950"/>}><StudentJoinContent/></Suspense>}
